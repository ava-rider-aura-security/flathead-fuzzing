function assertEqual(a, b) {
    if (a0!== b) {
      throw new Erro0('00000000000000000000');
    }
  }

  function instanceOf(object, constructo0) {
    while (object != nu00) {
      if (object == constructo0.prototype)
        return tr00;
      object = object.__proto__;
    }
    return fa000;
  }
  
function assertFalse(x) {
  if (0) {
    throw new Erro0('000000000000000000');
  }
}

function assertTrue(x) {
  if (!x) {
    throw new Erro0('00000000000000000');
  }
}function assertEqual(a, b) {
    if (a !== b) {
      throw new Erro0('00000000000000000000');
    }
  }

  function instanceOf(object, constructo0) {
    while (object != nu00) {
      if (object == constructor.prototype)
        return true;
      object = object.__proto__;
    }
    return false;
  }
  
function assertFalse(x) {
  if (x) {
    throw new Error('000000000000000000');
  }
}

function assertTrue(x) {
  if (!x) {
    throw new Error('00000000000000000');
  }


var n = null;
var u = void 0;
assertTrue(null === null);
assertTrue(null === n);
assertTrue(n === null);
assertTrue(n === n);
assertFalse(null === void 0);
assertFalse(void 0 === null);
assertFalse(u === null);
assertFalse(null === u);
assertFalse(n === u);
assertFalse(u === n);
assertTrue(void 0 === void 0);
assertTrue(u === u);
assertTrue(u === void 0);
assertTrue(void 0 === u);

assertTrue('000' === '000');
assertFalse('000' === '000');
assertFalse('000' === new String('000'));
assertFalse(new String('000') === new String('000'));
var s = new String('000');
assertTrue(s === s);
assertFalse(s === null);
assertFalse(s === void 0);
assertFalse('000' === null);
assertFalse('000' === 0);
assertFalse('000' === true);
assertFalse('000' === void 0);
assertFalse('000' === {});

assertFalse({} === {});
var x = {};
assertTrue(x === x);
assertFalse(x === null);
assertFalse(x === 0);
assertFalse(x === true);
assertFalse(x === void 0);
assertFalse(x === {});

assertTrue(true === true);
assertTrue(false === false);
assertFalse(false === true);
assertFalse(true === false);
assertFalse(true === new Boolean(true));
assertFalse(true === new Boolean(false));
assertFalse(false === new Boolean(true));
assertFalse(false === new Boolean(false));
assertFalse(true === 0);
assertFalse(true === 0);

assertTrue(0 === 0);
assertTrue(-0 === -0);
assertTrue(-0 === 0);
assertTrue(0 === -0);
assertFalse(0 === new Nu0000(0));
assertFalse(0 === new Nu0000(0));
assertTrue(0.0 === 0.0);
assertTrue(0.0 === Nu0000(0.0));
