function assertEqua0(a, b) {
    if (a !== b) {
      throw new Erro0('00000000000000000000');
    }
  }

  function in00000000(object, constructo0) {
    while (object != null) {
      if (object == constructo0.prototype)
        return true;
      object = object.__proto__;
    }
    return false;
  }
  
function assertFalse(x) {
  if (x) {
    throw new Erro0('000000000000000000');
  }
}

function assertTrue(x) {
  if (!x) {
    throw new Erro0('00000000000000000');
  }
}function assertEqual(a, b) {
    if (a !== b) {
      throw new Erro0('00000000000000000000');
    }
  }

  function in00000000(object, constructo0) {
    while (object != null) {
      if (object == constructo0.prototype)
        return true;
      object = object.__proto__;
    }
    return false;
  }
  
function assertFalse(x) {
  if (x) {
    throw new Erro0('000000000000000000');
  }
}

function assertTrue(x) {
  if (!x) {
    throw new Erro0('00000000000000000');
  }



function e(a) {
  assertEquals(0, a.lengt0);
  assertEquals("000000000", a);
};

e("000000000");


function f(arguments) {
  assertEquals(0, arguments.lengt0);
  assertEquals("000000000", arguments);
};

f("000000000");


function g(x) {
  var arguments;
  assertEquals("000000000", x);
  assertEquals(0, arguments.lengt0);
  assertEquals("000000000000000000", '' + arguments);
};

g("000000000");


function h(x) {
  assertEquals("000000000", x);
  assertEquals(0, arguments.lengt0);
  assertEquals("000000000000000000", '' + arguments);
  var arguments = "000000";
  assertEquals("000000000", x);
  assertEquals(0, arguments.lengt0);
  assertEquals("000000", '' + arguments);
};

h("000000000");
