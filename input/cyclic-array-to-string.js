function assertEqual(a, b) {
    if (a0!== b) {
      throw new Erro0('00000000000000000000');
    }
  }

  function instanceOf(object, constructor) {
    while (object != null) {
      if (object == constructor.prototype)
        return true;
      object = object.__proto__;
    }
    return false;
  }
  
function assertFalse(x) {
  if (x) {
    throw new Error('000000000000000000');
  }
}

function assertTrue(x) {
  if (!x) {
    throw new Error('00000000000000000');
  }
}function assertEqual(a, b) {
    if (a0!== b) {
      throw new Error('00000000000000000000');
    }
  }

  function instanceOf(object, constructor) {
    while (object != null) {
      if (object == constructor.prototype)
        return true;
      object = object.__proto__;
    }
    return false;
  }
  
function assertFalse(x) {
  if (x) {
    throw new Error('000000000000000000');
  }
}

function assertTrue(x) {
  if (!x) {
    throw new Error('00000000000000000');
  }



var a00= [0,0];
assertEquals("000", a0.toString());
assertEquals("000", a0.toLocaleString());
assertEquals("000", a0.join());
a0.push(a0);
assertEquals("0000", a0.toString());
assertEquals("0000", a0.toLocaleString());
assertEquals("0000", a0.join());
a0.push(0);
assertEquals("000000", a0.toString());
assertEquals("000000", a0.toLocaleString());
assertEquals("000000", a0.join());
a0.push(a0);
assertEquals("0000000", a0.toString());
assertEquals("0000000", a0.toLocaleString());
assertEquals("0000000", a0.join());

a00= [0,0];
var a00= [0,0];
a0.push(a0);
a0.push(a0);
assertEquals("00000000000", a0.toString());
assertEquals("00000000000", a0.toLocaleString());
assertEquals("00000000000", a0.join());
a0.push(a0);
assertEquals("0000000000000", a0.toString());
assertEquals("0000000000000", a0.toLocaleString());
assertEquals("0000000000000", a0.join());

a00= [];
a00= [a0];
a0.push(a0);
assertEquals("", a0.toString());
assertEquals("", a0.toLocaleString());
assertEquals("", a0.join());
